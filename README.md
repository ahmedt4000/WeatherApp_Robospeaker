# WeatherApp_Robospeaker
Takes user input for city name, Fetches real-time temperature using WeatherAPI, Converts the weather report into speech using pyttsx3, Gracefully exits the program when the user types "q".  Build With:- Python requests for API calls json for data parsing pyttsx3 for text-to-speech
